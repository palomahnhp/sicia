<div class="panel">
  <% Proposal.main_columns_automatic_sap.each_with_index do |field, index| %>
      <div class="medium-3 small-12 columns end" >
        <b><%= t("ransack.attributes.proposal.#{field}") %> : </b><%= proposal.send(field) %>
      </div>
  <% end %>
</div>
