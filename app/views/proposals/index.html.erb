<div class= "row" >
  <%= search_form_for @search do |f| %>
    <div class="large-4 small-9 columns">
      <%= f.text_field :title_cont, class: "radius-left expand", placeholder: "Título" %>
    </div>
    <div class="large-4 small-3 columns">
      <%= f.submit "Search", class: "radius-right button" %>
    </div>
  <% end %>
</div>

<div class= "row" >
  <div class= "small-12 columns">
    <h1><%= t('proposals.index.title') %> </h1>
  <table>
    <tr>
      <% Proposal.main_columns.each do |field| %>
        <th><%= t("model.proposal.#{field}") %></th>
      <% end %>
    </tr>
    <% @proposals.each do |proposal| %>
      <tr>
        <% Proposal.main_columns.each do |field| %>
          <td> <%= proposal.send(field) %></td>
        <% end %>
      </tr>
    <% end %>
  </table>
  </div>
</div>
